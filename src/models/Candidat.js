export class Candidat {
  id
  prenom
  nom
  age
  profession
  avatar

  constructor(id, prenom, nom, age, profession, avatar) {
    this.id = id
    this.prenom = prenom
    this.nom = nom
    this.age = age
    this.profession = profession
    this.avatar = avatar
  }
}
